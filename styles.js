import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    sectionContainer: {
        padding: 20,
        margin: 10,
        borderWidth: 2,
        borderColor: '#ccc',
        borderRadius: 5
    }
});