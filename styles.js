import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    sectionContainer: {
        padding: 20,
        margin: 10,
        borderWidth: 2,
        borderColor: '#ccc',
        borderRadius: 5
    },
    sequencesViewContainer: {
        padding: 20,
        margin: 5,
        borderWidth: 1,
        borderColor: '#ccc',
        borderRadius: 5
    }
});